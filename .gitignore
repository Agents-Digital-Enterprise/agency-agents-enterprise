# Secrets — never commit
.secrets/

# MCP config contains local paths and sensitive tokens — use mcp-config.example.json instead
.claude/mcp-config.json
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Environment files
.env
.env.*
!.env.example

# Node
node_modules/

# OS
.DS_Store
Thumbs.db
*:Zone.Identifier
